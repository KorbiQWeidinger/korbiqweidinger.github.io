# Notes / About

This folder demonstrates nested markdown content inside `public/contents`.

You can add more folders and files here, then update `public/contents/index.json` so they appear in the sidebar tree.
